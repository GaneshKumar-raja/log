# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LogisticDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks

  
  
      pod 'Cartography', '~> 2.0'
      pod 'Alamofire', '~> 4.0'
      pod 'Kingfisher' ,'~> 4.0'
      pod 'GoogleMaps'


  # Pods for LogisticDemo

  target 'LogisticDemoTests' do
    inherit! :search_paths
          pod 'Cartography', '~> 2.0'
    # Pods for testing
  end

  target 'LogisticDemoUITests' do
    inherit! :search_paths
          pod 'Cartography', '~> 2.0'
    # Pods for testing
  end

end
